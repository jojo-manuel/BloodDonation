apiVersion: apps/v1
kind: Deployment
metadata:
  name: blood-admin-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blood-admin-frontend
  template:
    metadata:
      labels:
        app: blood-admin-frontend
    spec:
      containers:
        - name: admin-frontend
          image: blood-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: blood-admin-frontend
spec:
  type: LoadBalancer
  selector:
    app: blood-admin-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
